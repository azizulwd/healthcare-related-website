import React from 'react';

const Service = (props) => {
    console.log(props.service);
    
    return (
        <div className="service">
            
        </div>
    );
};

export default Service;